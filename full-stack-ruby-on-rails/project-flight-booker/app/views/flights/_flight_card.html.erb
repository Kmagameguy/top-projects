<div class="flight-card">
  <div class="flight-header">
    <h3>Flight <%= flight.id %></h3>
  </div>
  <div class="flight-body">
    <p>From <%= flight.departure_airport_id %> to <%= flight.arrival_airport_id %></p>
    <p>Departs at: <%= flight.friendly_datetime %></p>
    <p>Seats remaining: <%= flight.num_tickets %></p>
  </div>
</div>
