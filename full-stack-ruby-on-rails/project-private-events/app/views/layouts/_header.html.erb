<header>
  <nav>
    <%= link_to("Home", root_path, method: :get, :class => "btn-home") %>
    <% if user_signed_in? %>
      <%= link_to("My Events", current_user, method: :get) %>
      <%= link_to("Log Out", destroy_user_session_path, data: { turbo_method: :delete }) %>
    <% else %>
      <%= link_to("Login", new_user_session_path, method: :get) %>
    <% end %>
  </nav>
</header>
