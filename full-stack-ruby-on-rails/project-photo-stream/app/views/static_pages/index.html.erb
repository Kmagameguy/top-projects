<h1>Flickr Photo Stream</h1>

<%= form_with url: static_pages_path, method: :get, local: true do |f| %>
  <%= f.text_field :username, placeholder: 'Search by Flickr Username' %>
  <%= f.submit "Search" %>
<% end %>

<% @photos.each do |photo| %>
  <%= image_tag Flickr.url(photo) %>
<% end %>
